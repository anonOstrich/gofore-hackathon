const nameService = {

  getAll:  () => {
  const result = Promise.resolve({})
  return result
},

  getByName:  (name) => {
  const result = Promise.resolve(7)
  return result
}
}

export default nameService